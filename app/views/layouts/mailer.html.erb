<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
    <title><%= yield(:title) || "Snap Mailer" %></title>

    <style>
      /* --- RESET --- */
      body, table, td, a {
        -webkit-text-size-adjust: 100%;
        -ms-text-size-adjust: 100%;
      }
      table, td {
        mso-table-lspace: 0pt;
        mso-table-rspace: 0pt;
      }
      img {
        -ms-interpolation-mode: bicubic;
      }

      /* --- BASE STYLES --- */
      body {
        margin: 0;
        padding: 0;
        background-color: #f6f8fb;
        font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
        color: #444;
      }

      .email-wrapper {
        width: 100%;
        padding: 40px 0;
      }

      .email-container {
        max-width: 600px;
        margin: 0 auto;
        background-color: #ffffff;
        border-radius: 10px;
        overflow: hidden;
        box-shadow: 0 6px 15px rgba(0, 0, 0, 0.05);
      }

      .email-header {
        background: linear-gradient(135deg, #4f46e5, #3b82f6);
        color: white;
        text-align: center;
        padding: 24px;
      }

      .email-header h1 {
        margin: 0;
        font-size: 22px;
        letter-spacing: 0.5px;
      }

      .email-body {
        padding: 32px 24px;
        font-size: 16px;
        line-height: 1.6;
      }

      .email-body h2 {
        color: #111827;
        font-size: 20px;
        margin-bottom: 12px;
      }

      .email-body p {
        margin-bottom: 16px;
      }

      .email-button {
        display: inline-block;
        background-color: #3b82f6;
        color: #ffffff;
        text-decoration: none;
        padding: 12px 24px;
        border-radius: 6px;
        font-weight: bold;
        margin: 16px 0;
      }

      .email-button:hover {
        background-color: #2563eb;
      }

      .divider {
        height: 1px;
        background-color: #e5e7eb;
        margin: 24px 0;
      }

      .email-footer {
        text-align: center;
        font-size: 13px;
        color: #6b7280;
        padding: 16px 24px 32px;
      }

      .email-footer a {
        color: #3b82f6;
        text-decoration: none;
      }

      .email-footer a:hover {
        text-decoration: underline;
      }
    </style>
  </head>

  <body>
    <div class="email-wrapper">
      <div class="email-container">
        <div class="email-header">
          <h1>Snap</h1>
        </div>

        <div class="email-body">
          <%= yield %>
        </div>

        <div class="divider"></div>

        <div class="email-footer">
          <p>© <%= Time.current.year %> Snap. All rights reserved.</p>
        </div>
      </div>
    </div>
  </body>
</html>
