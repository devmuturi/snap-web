<!DOCTYPE html>
<html>
  <head>
    <%= render "layouts/head" %>
  </head>

  <body>
    <%= render "shared/navbar" %>
    <%= render "shared/flashes"%>
    <%= render "shared/breadcrumbs" if breadcrumbs.show? %>
    <main>
      <%= yield %>
    </main>
  </body>
</html>
